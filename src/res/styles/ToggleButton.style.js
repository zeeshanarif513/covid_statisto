import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'center',
        backgroundColor: '#6d66ac',
        borderRadius: 40,
        height: 55
    },
    button: {
        flex: 1,
        padding: 10,
        borderRadius: 30,
        margin: 5,
        textAlign: 'center'
    }
});