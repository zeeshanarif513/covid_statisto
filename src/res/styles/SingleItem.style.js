import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container:  {
        flex: 1,
        minHeight: 60,
        justifyContent: 'center'
    },
    name: {
        color: 'white',
        fontSize: 20,
        fontWeight: 'bold'
    },
});